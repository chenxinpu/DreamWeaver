# 📋 官方平台App 产品需求文档（PRD）

---

## 一、文档概述

### 1.1 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 「织梦」官方平台App |
| 文档版本 | V1.0 |
| 文档状态 | 初稿 |
| 编写日期 | 2026年9月1日 |
| 目标平台 | iOS 15+ / Android 10+ / HarmonyOS Next |
| 产品定位 | 面向女性用户的一站式个性化服装设计+学习+创作者变现平台 |

### 1.2 项目背景

传统快时尚行业面临高库存、高退货、同质化严重三大痛点。本平台以"人人皆可参与设计、按需生产零库存"为核心理念，区别于SHEIN的批量快时尚模式，主打高品质定制化、用户共创生态与低门槛创作体验，打通"学习→设计→生产→变现"全链路。本PRD聚焦于**官方平台App**（非服装设计App），承载信息浏览、社交互动、交易履约、数据管理等核心场景。

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| 广场 | App内的社区信息流模块，用户发布推文、作品展示的公共空间 |
| 体模 | 基于用户体型数据生成的3D虚拟人台模型 |
| 作品集 | 创作者/品牌方发布的系列设计作品合集 |
| C2M | Customer-to-Manufacturer，用户直连制造的按需生产模式 |
| 柔性工厂 | 支持小批量、多品类、快速换线的智能制造工厂 |
| 设计能力认证 | 用户完成学习中心课程后获得的等级标识 |

---

## 二、产品定位与核心目标

### 2.1 产品定位

面向女性用户的一站式个性化服装设计生态平台官方App，是连接"消费者—创作者—供应链"三端的核心枢纽。承载内容社区、电商交易、学习成长、数据管理四大核心职能。

### 2.2 核心目标

| 目标维度 | 具体指标 | 目标值 |
|----------|----------|--------|
| 用户增长 | 上线6个月DAU | ≥50万 |
| 用户留存 | 次日留存率 / 7日留存率 | ≥45% / ≥25% |
| 内容生态 | 日均UGC推文发布量 | ≥1万条 |
| 交易转化 | 浏览→下单转化率 | ≥8% |
| 创作者活跃 | 月活跃创作者占比 | ≥15% |
| 用户满意度 | App Store评分 | ≥4.6 |

---

## 三、用户画像与使用场景

### 3.1 核心用户画像

#### 画像A：追求个性化的普通女性消费者（占比约60%）

- **年龄**：22-35岁
- **特征**：追求个性化穿搭、不愿撞衫，对服装品质有要求，愿意为定制化设计支付合理溢价
- **核心诉求**：浏览优质设计→3D预览效果→一键适配自身体型→下单定制→跟踪生产进度
- **关键行为**：浏览广场推文、收藏作品、查看热门榜单、进入创作者主页浏览作品集、3D虚拟试衣、下单购买、跟踪订单
- **注意事项**：已打版生产的定制衣物不支持退货

#### 画像B：设计爱好者/小白创作者（占比约25%）

- **年龄**：18-30岁
- **特征**：对服装设计感兴趣但缺乏专业技能和供应链资源，希望通过设计作品获得额外收入
- **核心诉求**：免费学习设计知识→使用工具创作→发布作品获取曝光→赚取佣金
- **关键行为**：学习中心课程学习、上传教程视频、查看后台数据（转化率/点击率/排名/成交量/退货率）、发布作品到广场、申请佣金提现

#### 画像C：专业设计师/小微品牌主理人（占比约15%）

- **年龄**：25-40岁
- **特征**：有设计能力和相关资质，希望获得低成本供应链支持和流量曝光，孵化个人品牌
- **核心诉求**：品牌展示→作品推广→订单管理→数据分析→品牌运营
- **关键行为**：管理品牌店铺、管理作品集、查看全维度KPI数据、处理售后、运营粉丝

### 3.2 核心使用场景

| 场景编号 | 场景名称 | 用户角色 | 场景描述 |
|----------|----------|----------|----------|
| S01 | 灵感发现 | 消费者 | 通勤路上刷广场推文，发现喜欢的连衣裙设计，点击进入作品集，切换3D背景预览效果，一键适配自身体模查看上身效果 |
| S02 | 榜单淘货 | 消费者 | 查看本周热门设计榜单，按品类/风格筛选，找到心仪设计后直接下单 |
| S03 | 订单跟踪 | 消费者 | 下单后在商城模块实时查看生产进度（裁剪中→缝制中→质检中→已发货），查看质检报告 |
| S04 | 技能学习 | 创作者 | 在学习中心完成"面料知识入门"课程，获得能力认证，解锁高级设计工具权限 |
| S05 | 数据复盘 | 创作者 | 进入后台查看本周作品数据：转化率3.2%、点击率12%、排名第56位、日成交量18单、退货率5.8% |
| S06 | 品牌运营 | 品牌方 | 管理品牌主页、上新作品、查看全维度经营数据、处理用户咨询 |

---

## 四、功能模块详细说明

### 4.1 整体信息架构

```
织梦App
├── 首页（广场）
│   ├── 推荐信息流（推文列表）
│   ├── 关注Tab
│   ├── 热门Tab
│   ├── 搜索入口
│   └── 推文详情页
│       ├── 图文/视频内容
│       ├── 关联作品链接
│       ├── 点赞/收藏/转发/评论
│       └── 跳转作品集
├── 榜单
│   ├── 周榜 / 月榜
│   ├── 品类筛选（连衣裙/衬衫/半裙/外套...）
│   ├── 风格筛选
│   └── 作品详情页
│       ├── 3D预览
│       ├── 一键适配体模
│       ├── 切换3D背景
│       └── 下单入口
├── 学习中心
│   ├── 课程分类（基础/进阶/高级）
│   ├── 课程详情页
│   ├── 视频播放器
│   ├── 实操练习
│   ├── 我的学习进度
│   ├── 上传教程入口（创作者）
│   └── 设计能力认证
├── 商城
│   ├── 首页推荐
│   ├── 分类浏览
│   ├── 商品详情页
│   ├── 购物车
│   ├── 下单结算
│   ├── 订单列表
│   ├── 订单详情（生产进度/质检报告/物流信息）
│   └── 售后管理
├── 我的
│   ├── 个人信息管理
│   │   ├── 体型数据采集（手动/AI拍照）
│   │   ├── 偏好标签管理
│   │   └── 收藏管理
│   ├── 我的作品集
│   ├── 我的关注
│   ├── 创作者后台（KPI数据面板）
│   ├── 品牌管理（品牌方）
│   ├── 消息中心
│   ├── 设置
│   └── 帮助与反馈
```

---

### 4.2 广场模块（首页信息流）

#### 4.2.1 功能说明

广场是App的核心内容消费场景，承载用户发布推文、浏览作品、社交互动的功能。

#### 4.2.2 页面结构

**广场首页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 顶部导航栏 | 搜索入口 + 消息入口 + 发布按钮 | 点击搜索进入全局搜索页；点击发布进入推文发布页 |
| Tab切换栏 | 推荐 / 关注 / 热门 | 默认展示"推荐"Tab，支持下拉刷新、上拉加载更多 |
| 信息流列表 | 推文卡片（封面图/视频缩略图 + 标题 + 作者头像/昵称 + 点赞数 + 关联作品标签） | 点击进入推文详情页 |
| 底部导航栏 | 首页 / 榜单 / 学习 / 商城 / 我的 | 当前Tab高亮 |

**推文详情页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 内容区 | 图文混排 / 视频播放 | 支持全屏播放视频 |
| 关联作品区 | 作者关联的设计作品/作品集卡片（最多展示3个） | 点击跳转至对应作品集或作品详情页 |
| 互动栏 | 点赞 / 收藏 / 转发 / 评论 | 点赞动画反馈；收藏状态同步到"我的收藏"；转发支持分享至微信/微博/复制链接 |
| 评论区 | 评论列表 + 评论输入框 | 支持回复、点赞评论 |
| 作者信息栏 | 头像 + 昵称 + 设计能力认证标识 + 关注按钮 | 点击头像进入作者主页 |

#### 4.2.3 推荐算法逻辑

- **冷启动阶段**（新用户前7天）：基于注册时选择的风格偏好标签（法式/韩系/简约/复古/街头等）推送内容
- **成熟阶段**：AI持续学习用户的浏览时长、收藏、购买、设计行为，动态调整推荐权重
- **内容质量过滤**：低质内容（模糊图片、搬运内容）自动降权；高互动内容（点赞率>5%）加权推荐

#### 4.2.4 推文发布功能

| 功能项 | 说明 |
|--------|------|
| 内容类型 | 支持图文（最多18张图 + 文字描述）、视频（最长3分钟） |
| 关联作品 | 发布时可链接自己的设计作品或作品集（从个人作品库选择） |
| 标签系统 | 支持添加设计元素标签（如#碎花 #泡泡袖 #法式）+ 自定义标签 |
| 发布审核 | AI自动审核（违规内容过滤）+ 人工复审（疑似违规内容），审核时效≤2小时 |

---

### 4.3 榜单模块

#### 4.3.1 功能说明

展示平台热门设计作品，帮助消费者发现优质设计，同时为创作者提供曝光渠道。

#### 4.3.2 页面结构

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 榜单类型Tab | 周榜 / 月榜 / 总榜 | 默认展示周榜 |
| 筛选栏 | 品类筛选（连衣裙/衬衫/半裙/外套/裤装/套装）+ 风格筛选 + 价格区间 | 支持多选组合筛选 |
| 榜单列表 | 排名序号 + 作品封面 + 作品名称 + 作者 + 热度值 + 销量 | 点击进入作品详情页 |
| 投票入口 | "为TA投票"按钮 | 投票后计入热度值，每位用户每天可投10票 |

#### 4.3.3 排名算法

- **热度值** = 投票数×0.3 + 收藏数×0.2 + 销量×0.3 + 互动率×0.2
- 每周/每月1日00:00重置对应榜单
- 防刷票机制：同一设备/账号/IP对同一作品每日限投1票

#### 4.3.4 作品详情页（从榜单进入）

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 3D预览区 | 作品3D模型展示，支持360°旋转、缩放 | 双指缩放、单指旋转 |
| 背景切换 | 提供6种预设3D背景（纯色/街拍/咖啡厅/海滩/工作室/自定义） | 点击切换，实时渲染 |
| 一键适配体模 | 将作品渲染到用户个人3D体模上 | 首次使用需完成体型数据采集；未采集用户引导跳转采集页 |
| 作品信息 | 名称、作者、品类、面料、价格、销量 | — |
| 操作栏 | 收藏 / 分享 / 立即购买 | 点击"立即购买"进入下单流程 |

> **关键交互说明**：用户从他人作品集进入作品详情页后，可通过"一键适配体模"功能将作品切换为自己的体型预览效果，并支持切换3D背景，实现"所见即所得"的沉浸式体验。

---

### 4.4 学习中心模块

#### 4.4.1 功能说明

提供从服装设计基础到高级技能的阶梯式免费教程体系，支持用户自主学习并获取认证，同时支持创作者上传教程内容。

#### 4.4.2 课程体系

| 层级 | 课程方向 | 示例课程 | 完成奖励 |
|------|----------|----------|----------|
| L1 入门 | 服装设计基础、色彩理论、面料认知 | "认识100种常见面料"、"色彩搭配入门" | 获得"设计学徒"认证 |
| L2 进阶 | 打版技巧、工艺知识、设计软件操作 | "连衣裙打版实操"、"缝纫工艺入门" | 获得"设计师"认证，解锁高级设计工具 |
| L3 高级 | 趋势分析、品牌运营、商业变现 | "如何打造爆款设计"、"个人品牌运营指南" | 获得"资深设计师"认证，解锁品牌孵化资格 |

#### 4.4.3 页面结构

**学习中心首页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 我的学习卡片 | 当前等级 + 学习进度 + 已获认证 | 点击查看详情 |
| 推荐课程 | 基于用户等级和兴趣推荐的课程卡片 | 点击进入课程详情 |
| 分类导航 | 设计基础 / 面料知识 / 打版技巧 / 软件操作 / 趋势分析 / 品牌运营 | 点击筛选对应分类 |
| 热门教程 | 用户/创作者上传的热门教程视频 | 按播放量排序 |

**课程详情页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 课程信息 | 标题、讲师、时长、难度、学习人数 | — |
| 视频播放器 | 支持倍速（0.5x/1.0x/1.5x/2.0x）、画中画、后台播放 | 记录播放进度，下次续播 |
| 课程大纲 | 章节目录，标记已学/未学状态 | 点击跳转对应章节 |
| 实操练习 | 配套练习任务（如"设计一件连衣裙并上传"） | 完成后获得积分 |
| 笔记区 | 用户个人学习笔记 | 支持图文编辑 |

#### 4.4.4 教程上传功能（创作者）

| 功能项 | 说明 |
|--------|------|
| 上传资格 | 拥有"设计师"及以上认证的用户 |
| 内容格式 | 视频（MP4，最长60分钟，≤2GB）+ 图文说明 |
| 审核流程 | AI自动审核（违规内容检测）→ 人工审核（内容质量评估），审核时效≤24小时 |
| 收益模式 | 教程播放量达到1万次以上可申请流量分成 |
| 分类标签 | 上传时需选择课程分类和难度等级 |

---

### 4.5 创作者后台模块

#### 4.5.1 功能说明

为创作者和品牌方提供全方位的数据分析面板，帮助其了解作品表现并优化运营策略。

#### 4.5.2 数据面板

**核心KPI指标**

| 指标名称 | 计算方式 | 展示维度 |
|----------|----------|----------|
| 转化率 | 下单用户数 / 作品详情页访问用户数 × 100% | 实时 / 日 / 周 / 月 |
| 点击率 | 作品被点击次数 / 作品曝光次数 × 100% | 实时 / 日 / 周 / 月 |
| 排名 | 基于热度值的综合排名 | 周榜排名 / 月榜排名 / 总榜排名 |
| 成交量 | 累计/周期内成功支付的订单数 | 每日 / 每月 / 累计 |
| 退货率 | 退货订单数 / 总成交订单数 × 100% | 实时同步 |

**数据可视化**

| 图表类型 | 展示内容 |
|----------|----------|
| 折线图 | 近7天/30天成交量趋势、转化率趋势 |
| 柱状图 | 各作品销量对比 |
| 饼图 | 用户来源渠道分布（广场推荐/榜单/搜索/分享） |
| 热力图 | 作品详情页用户浏览热力分布 |

#### 4.5.3 后台页面结构

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 数据概览卡片 | 今日成交额 / 今日访客 / 转化率 / 排名 | 点击展开详细数据 |
| 作品列表 | 各作品的核心数据（销量/转化率/退货率/排名） | 点击单作品查看明细 |
| 趋势图表区 | 可选时间维度的趋势图 | 支持切换指标和时间范围 |
| 退货分析 | 退货率趋势 + 退货原因分布 | 退货率>10%时红色预警提示 |
| 佣金明细 | 可提现金额 / 待结算金额 / 历史提现记录 | 申请提现入口 |

#### 4.5.4 佣金结算规则

- 进入投票前1000名 且 作品退货率<10%的设计师，可按订单数的8%-12%抽取佣金
- 佣金比例根据品类和订单量阶梯调整（订单量越大，比例越高）
- 结算周期：T+7（订单确认收货后7天结算到个人账户）
- 提现门槛：≥100元可申请提现，支持提现至银行卡/支付宝

---

### 4.6 商城模块

#### 4.6.1 功能说明

参考主流购物平台（如淘宝、小红书）的设计思路，提供完整的电商交易闭环，并深度集成C2M按需生产的全流程跟踪能力。

#### 4.6.2 页面结构

**商城首页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 搜索栏 | 支持搜索作品/设计师/品牌/品类 | 支持搜索历史和热搜推荐 |
| Banner轮播 | 运营活动/新品推荐/热门榜单入口 | 自动轮播，支持手动滑动 |
| 品类导航 | 连衣裙/衬衫/半裙/外套/裤装/套装/配饰 | 图标+文字，点击进入分类页 |
| 推荐信息流 | 基于用户偏好的商品推荐（瀑布流） | 下拉刷新，上拉加载 |

**商品详情页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 3D展示区 | 作品3D模型 + 背景切换 + 一键适配体模 | 同榜单作品详情页交互 |
| 商品信息 | 名称、设计师/品牌、价格、面料成分、可选颜色/尺码 | — |
| 尺码推荐 | 基于用户体型数据智能推荐尺码 | 未采集体型数据时引导采集 |
| 设计师信息 | 头像、昵称、认证标识、粉丝数、其他作品 | 点击进入设计师主页 |
| 用户评价 | 已购用户的评价（含试穿图） | 支持筛选有图评价 |
| 操作栏 | 收藏 / 加入购物车 / 立即购买 | 定制类商品直接进入下单流程 |

**下单结算页**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 商品信息 | 选中商品缩略图、名称、规格、数量 | — |
| 体型确认 | 展示当前体型数据，确认或修改 | 已打版商品提示"定制商品不支持退货" |
| 收货地址 | 选择/新增收货地址 | — |
| 优惠信息 | 可用优惠券/平台补贴 | 自动匹配最优优惠 |
| 支付栏 | 应付总额 + 支付方式（微信/支付宝/银行卡） | 确认支付 |

**订单管理**

| 订单状态 | 说明 | 用户可操作 |
|----------|------|------------|
| 待支付 | 订单已创建，等待支付 | 支付 / 取消订单 |
| 待生产 | 已支付，等待排产 | 查看详情 |
| 生产中 | 工厂正在制作 | 查看生产进度详情 |
| 质检中 | 成品质量检验 | 查看质检报告 |
| 待发货 | 质检通过，等待发货 | — |
| 已发货 | 已交付物流 | 查看物流信息 |
| 已收货 | 用户确认收货 | 评价 / 申请售后 |
| 已完成 | 订单完结 | 追评 |

**订单详情页（核心差异化功能）**

| 区域 | 内容 | 交互说明 |
|------|------|----------|
| 状态时间轴 | 可视化展示订单全流程节点及时间 | 当前节点高亮，已完成节点显示完成时间 |
| 生产进度 | 当前环节 + 进度百分比 + 预计完成时间 | 生产中状态实时更新 |
| 质检报告 | 面料检测报告 + 工艺检测 + 尺寸偏差 + 质检图片 | 质检通过后展示，支持查看大图 |
| 物流信息 | 快递公司 + 运单号 + 物流轨迹 | 支持一键复制运单号 |
| 售后入口 | 退货/换货/退款申请 | 已打版定制商品仅支持质量问题退货 |

#### 4.6.3 退货规则说明

- **已打版生产的定制衣物不支持退货**（下单时需用户明确确认）
- 非定制类商品（如配饰、面料样品）支持7天无理由退货
- 质量问题（面料瑕疵、尺寸偏差>5mm、工艺缺陷）支持退货退款，运费由平台承担

---

### 4.7 个人信息管理模块

#### 4.7.1 体型数据采集

**方式一：手动输入**

| 指标项 | 单位 | 输入方式 | 校验规则 |
|--------|------|----------|----------|
| 身高 | cm | 数字输入 | 140-200 |
| 体重 | kg | 数字输入 | 35-150 |
| 胸围 | cm | 数字输入 | 60-130 |
| 下胸围 | cm | 数字输入 | 55-110 |
| 腰围 | cm | 数字输入 | 50-120 |
| 臀围 | cm | 数字输入 | 60-130 |
| 肩宽 | cm | 数字输入 | 30-55 |
| 臂长 | cm | 数字输入 | 45-75 |
| 大腿围 | cm | 数字输入 | 35-75 |
| 小腿围 | cm | 数字输入 | 25-50 |
| 颈围 | cm | 数字输入 | 28-45 |
| 背长 | cm | 数字输入 | 35-55 |

**方式二：AI拍照量体**

| 步骤 | 说明 |
|------|------|
| 拍摄引导 | 引导用户穿着贴身衣物，在纯色背景前拍摄正面、侧面、背面3张照片 |
| AI识别 | 自动识别100+身体数据点，精度控制在±3mm内 |
| 数据确认 | 展示识别结果，用户可手动微调个别数据 |
| 数据加密 | 体型数据AES-256加密存储，仅用于版型匹配，不对外共享 |

#### 4.7.2 偏好与收藏管理

| 功能项 | 说明 |
|--------|------|
| 设计偏好标签 | 用户可选择/修改风格偏好标签（法式/韩系/简约/复古/街头/甜美/知性等） |
| 收藏管理 | 分类管理收藏的推文、作品、达人主页，支持创建自定义收藏夹 |
| 浏览历史 | 记录近期浏览的作品/推文，支持清除 |

---

### 4.8 品牌孵化通道

#### 4.8.1 申请条件

| 条件项 | 要求 |
|--------|------|
| 资质要求 | 持有设计专利或营业执照 |
| 团队要求 | 拥有运营团队（至少1名运营人员） |
| 作品要求 | 在平台发布≥20件原创设计作品 |
| 认证要求 | 持有"资深设计师"认证 |

#### 4.8.2 品牌方权益

- 创建个人品牌主页（自定义装修）
- 自主定价、管理店铺
- 获得供应链支持（优先排产、面料直采优惠）
- 获得流量扶持（首页推荐位、榜单加权）
- 获得品牌运营指导（专属运营经理1对1）

---

## 五、交互流程设计

### 5.1 核心用户旅程

#### 消费者购买旅程

```
打开App → 浏览广场推荐/榜单 → 发现心仪作品
→ 进入作品详情页 → 切换3D背景预览 → 一键适配自身体模
→ 确认满意 → 选择尺码（智能推荐）→ 确认体型数据
→ 确认"定制商品不支持退货"提示 → 支付
→ 查看生产进度 → 查看质检报告 → 确认收货 → 评价
```

#### 创作者变现旅程

```
注册 → 完善个人信息 → 进入学习中心
→ 完成基础课程 → 获得"设计学徒"认证
→ 使用设计工具创作（服装设计App）→ 上传作品到个人作品页
→ 发布推文到广场（关联作品）→ 获取曝光和互动
→ 作品进入榜单 → 获得销量
→ 查看后台数据 → 优化设计方向
→ 满足条件后申请佣金提现
```

### 5.2 关键交互原型说明

#### 作品详情页交互流程

```
┌─────────────────────────────────────────┐
│  作品详情页                               │
│  ┌─────────────────────────────────┐    │
│  │  3D预览区                        │    │
│  │  [3D模型展示，支持旋转/缩放]      │    │
│  │  ┌──────┐ ┌──────┐ ┌──────┐    │    │
│  │  │背景1 │ │背景2 │ │背景3 │    │    │
│  │  └──────┘ └──────┘ └──────┘    │    │
│  │  [🔄 一键适配我的体模]           │    │
│  └─────────────────────────────────┘    │
│  作品名称：法式碎花泡泡袖连衣裙           │
│  设计师：@小织  ⭐资深设计师              │
│  价格：¥328  销量：1,286                 │
│  面料：100%真丝  工艺：法式缝             │
│  ┌──────────────────────────────────┐   │
│  │ [❤️收藏]  [📤分享]  [🛒立即购买]  │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 六、技术架构设计

### 6.1 整体技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层 (Client)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐    │
│  │  iOS App     │  │ Android App  │  │ HarmonyOS App      │    │
│  │  (Swift/     │  │ (Kotlin/     │  │ (ArkTS/            │    │
│  │   SwiftUI)   │  │  Compose)    │  │   ArkUI)           │    │
│  └──────┬───────┘  └──────┬───────┘  └────────┬───────────┘    │
│         │                 │                    │                 │
│  ┌──────┴─────────────────┴────────────────────┴───────────┐    │
│  │              跨平台业务层 (Flutter/Dart)                  │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │    │
│  │  │广场模块│ │榜单模块│ │学习模块│ │商城模块│           │    │
│  │  └────────┘ └────────┘ └────────┘ └────────┘           │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │    │
│  │  │用户模块│ │后台模块│ │3D渲染  │ │AI服务  │           │    │
│  │  └────────┘ └────────┘ └────────┘ └────────┘           │    │
│  └───────────────────────┬─────────────────────────────────┘    │
└──────────────────────────┼──────────────────────────────────────┘
                           │ HTTPS / WebSocket
┌──────────────────────────┼──────────────────────────────────────┐
│                     网关层 (Gateway)                             │
│  ┌───────────────────────┴──────────────────────────────────┐   │
│  │  API Gateway (Kong/APISIX)                                │   │
│  │  · 统一鉴权 (JWT + OAuth 2.0)                             │   │
│  │  · 流量控制 / 限流熔断                                     │   │
│  │  · 请求路由 / 负载均衡                                     │   │
│  │  · 日志采集 / 链路追踪                                     │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────┼──────────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────────┐
│                   微服务层 (Microservices)                       │
│                                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │用户服务   │ │内容服务   │ │交易服务   │ │推荐服务   │          │
│  │·注册登录  │ │·推文管理  │ │·订单管理  │ │·个性化推荐│          │
│  │·体型数据  │ │·评论管理  │ │·支付对接  │ │·榜单计算  │          │
│  │·偏好管理  │ │·投票管理  │ │·佣金结算  │ │·趋势预测  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│                                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │供应链服务 │ │学习服务   │ │品牌服务   │ │搜索服务   │          │
│  │·生产跟踪  │ │·课程管理  │ │·品牌管理  │ │·全文检索  │          │
│  │·质检管理  │ │·认证管理  │ │·店铺管理  │ │·智能联想  │          │
│  │·物流对接  │ │·教程上传  │ │·资质审核  │ │·搜索排序  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│                                                                  │
│  ┌──────────────────────┐  ┌──────────────────────┐            │
│  │  AI服务               │  │  3D渲染服务           │            │
│  │  ·AI拍照量体          │  │  ·体模生成             │            │
│  │  ·内容审核            │  │  ·面料物理模拟         │            │
│  │  ·智能推荐            │  │  ·实时渲染             │            │
│  └──────────────────────┘  └──────────────────────┘            │
└──────────────────────────┼──────────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────────┐
│                     数据层 (Data)                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ MySQL    │ │ MongoDB  │ │ Redis    │ │ ES       │          │
│  │ 结构化数据│ │ 非结构化  │ │ 缓存/    │ │ 全文搜索 │          │
│  │ (用户/   │ │ 数据     │ │ 会话/    │ │          │          │
│  │  订单)   │ │ (推文/   │ │ 排行榜)  │ │          │          │
│  │          │ │  作品)   │ │          │ │          │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────┐          │
│  │ OSS      │ │ Kafka    │ │ 消息队列              │          │
│  │ 文件存储  │ │ 事件总线  │ │ (RabbitMQ)           │          │
│  └──────────┘ └──────────┘ └──────────────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 客户端技术选型

#### 6.2.1 选型方案

考虑到平台需要覆盖iOS、Android、HarmonyOS三端，且业务包含3D渲染、视频播放、实时通信等高性能场景，推荐采用**"原生+跨平台"混合架构**：

| 层级 | 技术方案 | 说明 |
|------|----------|------|
| 核心业务页面 | Flutter 5.x (Dart) | 广场、榜单、学习中心、商城、后台等标准业务页面，一套代码三端运行 |
| 3D渲染模块 | 原生开发（Swift/Metal + Kotlin/Vulkan + ArkTS/ArkUI） | 3D虚拟试衣、体模渲染等高性能场景，需调用GPU底层能力 |
| AI拍照量体 | 原生开发 + 端侧AI推理 | 调用设备NPU/GPU进行实时图像处理 |
| 视频播放 | 原生播放器封装 | 保障各平台视频解码性能和兼容性 |

#### 6.2.2 选型理由

- **Flutter优势**：自渲染引擎（Impeller）在复杂UI动画场景下帧率稳定性优异，2026年5月基于iPhone 17 Pro、Google Pixel 10、小米16 Ultra等主流旗舰设备的测试数据显示，其复杂列表帧率可达约119 fps，冷启动时间约0.9s；Dart语言学习曲线平缓，适合团队快速上手；支持WebAssembly编译，网页端性能接近原生
- **原生模块必要性**：3D渲染需要直接调用Metal/Vulkan等GPU API，Flutter的Platform Channel通信开销在此场景不可接受；AI量体需要调用CoreML/NNAPI等端侧推理框架
- **HarmonyOS适配**：Flutter对鸿蒙Next的适配在持续推进中，3D和AI模块通过ArkTS原生实现确保最佳体验

#### 6.2.3 客户端分层架构

```
┌─────────────────────────────────────────────┐
│              表现层 (Presentation)            │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │  Pages  │ │ Widgets │ │ Themes  │      │
│  │ (页面)  │ │ (组件)  │ │ (主题)  │      │
│  └─────────┘ └─────────┘ └─────────┘      │
├─────────────────────────────────────────────┤
│              业务逻辑层 (BLoC)               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │ BLoC    │ │ Event   │ │ State   │      │
│  │(控制器) │ │ (事件)  │ │ (状态)  │      │
│  └─────────┘ └─────────┘ └─────────┘      │
├─────────────────────────────────────────────┤
│              数据层 (Data)                   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │Repository│ │Local   │ │Remote  │      │
│  │(数据仓库)│ │Cache   │ │API     │      │
│  └─────────┘ └─────────┘ └─────────┘      │
├─────────────────────────────────────────────┤
│              基础设施层 (Infrastructure)      │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │Network  │ │Storage  │ │Native  │      │
│  │(网络)   │ │(存储)   │ │Bridge  │      │
│  └─────────┘ └─────────┘ └─────────┘      │
└─────────────────────────────────────────────┘
```

#### 6.2.4 关键技术细节

**状态管理**：采用BLoC (Business Logic Component) 模式

- 事件驱动：UI发送Event → BLoC处理业务逻辑 → 输出State → UI响应式更新
- 优势：业务逻辑与UI完全解耦，便于单元测试
- 全局状态（用户登录态、体型数据）通过`MultiBlocProvider`注入

**网络层**：

- HTTP客户端：Dio（支持拦截器、取消请求、FormData上传）
- 请求拦截器：自动附加JWT Token、设备信息、版本号
- 响应拦截器：统一错误码处理、Token过期自动刷新
- WebSocket：用于订单状态实时推送、消息通知

**本地存储**：

- 轻量数据（用户设置、Token）：shared_preferences
- 结构化缓存（浏览历史、草稿）：sqflite（SQLite）
- 文件缓存（图片、3D模型）：本地文件系统 + LRU淘汰策略

**3D渲染桥接**：

- Flutter通过Platform Channel调用原生3D渲染模块
- 3D模型格式：glTF 2.0（通用交换格式）
- 面料物理模拟：基于质点弹簧模型（Mass-Spring Model），模拟真丝/棉麻/羊毛等不同面料的垂坠感
- 渲染帧率目标：≥30fps（中端设备）、≥60fps（旗舰设备）

### 6.3 后端技术架构

#### 6.3.1 微服务划分

| 服务名称 | 职责 | 技术栈 | 数据库 |
|----------|------|--------|--------|
| 用户服务 (user-service) | 注册登录、个人信息、体型数据、偏好管理 | Spring Boot 3.x + Kotlin | MySQL + Redis |
| 内容服务 (content-service) | 推文CRUD、评论、投票、收藏 | Spring Boot 3.x + Kotlin | MySQL + MongoDB + Redis |
| 交易服务 (trade-service) | 订单管理、支付对接、佣金结算 | Spring Boot 3.x + Kotlin | MySQL + Redis |
| 供应链服务 (supply-service) | 生产进度、质检管理、物流对接 | Spring Boot 3.x + Kotlin | MySQL + RabbitMQ |
| 学习服务 (learn-service) | 课程管理、学习进度、认证、教程上传 | Spring Boot 3.x + Kotlin | MySQL + MongoDB |
| 推荐服务 (recommend-service) | 个性化推荐、榜单计算、趋势预测 | Python (FastAPI) | Redis + MongoDB |
| 搜索服务 (search-service) | 全文检索、智能联想、搜索排序 | Go (Gin) | Elasticsearch |
| AI服务 (ai-service) | AI量体、内容审核、智能推荐模型 | Python (FastAPI + PyTorch) | Redis + MinIO |
| 3D渲染服务 (render-service) | 体模生成、面料模拟、实时渲染 | C++ + WebGL | MinIO |
| 品牌服务 (brand-service) | 品牌管理、店铺管理、资质审核 | Spring Boot 3.x + Kotlin | MySQL |

#### 6.3.2 服务通信

| 通信方式 | 场景 | 技术方案 |
|----------|------|----------|
| 同步调用 | 服务间实时数据查询 | gRPC（内部服务间）/ RESTful API（对外） |
| 异步通信 | 订单状态变更通知、生产进度更新 | Kafka（高吞吐事件流）+ RabbitMQ（业务消息） |
| 实时推送 | 订单进度、消息通知 | WebSocket + MQTT |

#### 6.3.3 数据库设计

**核心数据表（MySQL）**

```sql
-- 用户表
CREATE TABLE `user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `phone` VARCHAR(20) UNIQUE NOT NULL,
    `nickname` VARCHAR(50),
    `avatar_url` VARCHAR(500),
    `gender` TINYINT DEFAULT 1 COMMENT '1-女 2-男',
    `role` TINYINT DEFAULT 1 COMMENT '1-消费者 2-创作者 3-品牌方',
    `certification_level` TINYINT DEFAULT 0 COMMENT '0-无 1-学徒 2-设计师 3-资深',
    `status` TINYINT DEFAULT 1 COMMENT '1-正常 0-禁用',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 体型数据表
CREATE TABLE `body_measurement` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL,
    `height` DECIMAL(5,1) COMMENT '身高cm',
    `weight` DECIMAL(5,1) COMMENT '体重kg',
    `bust` DECIMAL(5,1) COMMENT '胸围cm',
    `under_bust` DECIMAL(5,1) COMMENT '下胸围cm',
    `waist` DECIMAL(5,1) COMMENT '腰围cm',
    `hip` DECIMAL(5,1) COMMENT '臀围cm',
    `shoulder_width` DECIMAL(5,1) COMMENT '肩宽cm',
    `arm_length` DECIMAL(5,1) COMMENT '臂长cm',
    `thigh` DECIMAL(5,1) COMMENT '大腿围cm',
    `calf` DECIMAL(5,1) COMMENT '小腿围cm',
    `neck` DECIMAL(5,1) COMMENT '颈围cm',
    `back_length` DECIMAL(5,1) COMMENT '背长cm',
    `source` TINYINT COMMENT '1-手动输入 2-AI拍照',
    `encrypted_data` TEXT COMMENT '加密的完整数据点',
    `version` INT DEFAULT 1,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_user_id` (`user_id`)
);

-- 作品表
CREATE TABLE `design_work` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `creator_id` BIGINT NOT NULL,
    `brand_id` BIGINT COMMENT '关联品牌ID',
    `title` VARCHAR(200) NOT NULL,
    `category` VARCHAR(50) COMMENT '品类',
    `style_tags` JSON COMMENT '风格标签',
    `fabric_type` VARCHAR(100) COMMENT '面料类型',
    `price` DECIMAL(10,2),
    `model_3d_url` VARCHAR(500) COMMENT '3D模型文件地址',
    `cover_url` VARCHAR(500),
    `status` TINYINT DEFAULT 1 COMMENT '1-上架 0-下架 2-审核中',
    `view_count` INT DEFAULT 0,
    `collect_count` INT DEFAULT 0,
    `sales_count` INT DEFAULT 0,
    `return_rate` DECIMAL(5,2) DEFAULT 0 COMMENT '退货率%',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_creator` (`creator_id`),
    INDEX `idx_category` (`category`),
    INDEX `idx_status` (`status`)
);

-- 订单表
CREATE TABLE `order` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `order_no` VARCHAR(32) UNIQUE NOT NULL,
    `user_id` BIGINT NOT NULL,
    `work_id` BIGINT NOT NULL,
    `creator_id` BIGINT NOT NULL,
    `total_amount` DECIMAL(10,2),
    `status` TINYINT DEFAULT 0 COMMENT '0-待支付 1-待生产 2-生产中 3-质检中 4-待发货 5-已发货 6-已收货 7-已完成',
    `body_data_snapshot` JSON COMMENT '下单时体型数据快照',
    `size_recommend` VARCHAR(50),
    `is_custom_made` TINYINT DEFAULT 1 COMMENT '是否定制(1-是 0-否)',
    `return_status` TINYINT DEFAULT 0 COMMENT '0-无 1-退货中 2-已退货',
    `address_snapshot` JSON,
    `paid_at` DATETIME,
    `shipped_at` DATETIME,
    `received_at` DATETIME,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_user` (`user_id`),
    INDEX `idx_creator` (`creator_id`),
    INDEX `idx_status` (`status`),
    INDEX `idx_created` (`created_at`)
);

-- 推文表
CREATE TABLE `post` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `author_id` BIGINT NOT NULL,
    `content` TEXT,
    `media_urls` JSON COMMENT '图片/视频URL列表',
    `media_type` TINYINT COMMENT '1-图文 2-视频',
    `linked_works` JSON COMMENT '关联作品ID列表',
    `tags` JSON COMMENT '标签列表',
    `like_count` INT DEFAULT 0,
    `collect_count` INT DEFAULT 0,
    `comment_count` INT DEFAULT 0,
    `share_count` INT DEFAULT 0,
    `audit_status` TINYINT DEFAULT 0 COMMENT '0-待审核 1-通过 2-拒绝',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_author` (`author_id`),
    INDEX `idx_created` (`created_at`)
);

-- 课程表
CREATE TABLE `course` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `title` VARCHAR(200) NOT NULL,
    `instructor_id` BIGINT,
    `category` VARCHAR(50),
    `level` TINYINT COMMENT '1-入门 2-进阶 3-高级',
    `video_url` VARCHAR(500),
    `duration` INT COMMENT '时长(秒)',
    `cover_url` VARCHAR(500),
    `description` TEXT,
    `learner_count` INT DEFAULT 0,
    `is_user_uploaded` TINYINT DEFAULT 0 COMMENT '是否用户上传',
    `status` TINYINT DEFAULT 1,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_category` (`category`),
    INDEX `idx_level` (`level`)
);

-- 榜单热度表
CREATE TABLE `ranking` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `work_id` BIGINT NOT NULL,
    `period_type` TINYINT COMMENT '1-周榜 2-月榜 3-总榜',
    `period_start` DATE,
    `vote_score` DECIMAL(10,2) DEFAULT 0,
    `collect_score` DECIMAL(10,2) DEFAULT 0,
    `sales_score` DECIMAL(10,2) DEFAULT 0,
    `interaction_score` DECIMAL(10,2) DEFAULT 0,
    `total_score` DECIMAL(10,2) DEFAULT 0,
    `rank_no` INT,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_period` (`period_type`, `period_start`),
    INDEX `idx_score` (`total_score` DESC)
);
```

**MongoDB 集合设计**

```javascript
// 推文内容集合（存储富文本、评论等灵活结构）
db.post_content.insertOne({
    post_id: NumberLong("10001"),
    rich_content: { /* 富文本内容 */ },
    comments: [
        {
            user_id: NumberLong("2001"),
            content: "太好看了！",
            created_at: ISODate("2026-09-01T10:00:00Z"),
            likes: 5
        }
    ],
    audit_log: {
        ai_result: "pass",
        manual_review: null,
        reviewed_at: null
    }
});

// 推荐用户画像集合
db.user_profile.insertOne({
    user_id: NumberLong("1001"),
    style_preferences: ["法式", "简约", "碎花"],
    browse_history: [{ work_id: 10001, duration: 45, timestamp: ISODate() }],
    collect_history: [10001, 10005, 10012],
    purchase_history: [10001],
    design_behavior: { has_designed: true, tools_used: ["参数化设计", "AI辅助"] },
    feature_vector: [0.12, 0.85, 0.33, ...],  // 推荐模型特征向量
    updated_at: ISODate()
});
```

### 6.4 关键技术实现方案

#### 6.4.1 3D虚拟试衣技术

| 技术环节 | 方案 | 说明 |
|----------|------|------|
| 体模生成 | 基于12项体型参数 + 100+ AI数据点，生成参数化人体模型 | 使用SMPL-X人体模型作为基础，通过BlendShape实现体型变形 |
| 面料模拟 | 质点弹簧模型 + GPU并行计算 | 不同面料设定不同物理参数（ stiffness/damping/mass），真丝垂坠感强、棉麻挺括 |
| 实时渲染 | 基于PBR（物理渲染）材质系统 | 支持面料光泽度、透明度、纹理细节的真实呈现 |
| 背景切换 | 预置HDR环境贴图 + 实时光照 | 6种预设场景，支持自定义上传背景 |
| 性能优化 | LOD（多细节层次）+ 遮挡剔除 + 纹理压缩 | 中端设备≥30fps，旗舰设备≥60fps |

#### 6.4.2 AI拍照量体技术

```
用户拍摄3张照片（正面/侧面/背面）
        │
        ▼
┌───────────────────┐
│ 图像预处理         │  · 人体检测（YOLOv8）
│                    │  · 背景去除（RMBG）
│                    │  · 关键点定位（17个身体关键点）
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│ 3D人体重建         │  · 基于关键点估计3D姿态
│                    │  · SMPL-X模型拟合
│                    │  · 提取100+身体数据点
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│ 精度校验           │  · 与手动输入数据交叉验证
│                    │  · 异常值检测（偏差>2cm提示重拍）
│                    │  · 精度目标：±3mm
└────────┬──────────┘
         │
         ▼
    加密存储 + 生成体模
```

#### 6.4.3 个性化推荐算法

| 推荐策略 | 权重 | 说明 |
|----------|------|------|
| 协同过滤（User-CF） | 30% | 基于相似用户的行为推荐 |
| 内容推荐 | 25% | 基于作品标签与用户偏好标签匹配 |
| 热度推荐 | 20% | 基于实时热度的推荐 |
| 探索推荐 | 15% | 随机探索用户未接触过的品类/风格 |
| 实时行为修正 | 10% | 基于最近5分钟行为的实时调整 |

#### 6.4.4 订单全流程状态机

```
待支付 ──支付成功──→ 待生产 ──排产确认──→ 生产中
  │                                        │
  │超时未支付                               │生产完成
  ▼                                        ▼
已取消                                   质检中 ──质检通过──→ 待发货 ──发货──→ 已发货 ──确认收货──→ 已完成
                                           │                                    │
                                           │质检不通过                           │申请售后
                                           ▼                                    ▼
                                       生产中(返工)                           售后处理中
```

---

## 七、接口设计规范

### 7.1 接口规范总则

- 协议：HTTPS
- 数据格式：JSON
- 认证方式：JWT Bearer Token
- 版本管理：URL路径版本号（如 `/api/v1/`）
- 分页规范：`page`（页码）+ `pageSize`（每页条数），默认 pageSize=20
- 错误码规范：

| 错误码 | 含义 |
|--------|------|
| 0 | 成功 |
| 1001 | 参数校验失败 |
| 2001 | 未登录/Token过期 |
| 2002 | 权限不足 |
| 3001 | 资源不存在 |
| 4001 | 业务规则校验失败 |
| 5001 | 服务内部错误 |

### 7.2 核心接口列表

#### 广场模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取推荐信息流 | GET | `/api/v1/feed/recommend` | 支持分页，返回推文列表 |
| 获取关注信息流 | GET | `/api/v1/feed/following` | 关注用户发布的推文 |
| 获取热门信息流 | GET | `/api/v1/feed/hot` | 按热度排序 |
| 发布推文 | POST | `/api/v1/post/create` | 支持图文/视频，关联作品 |
| 推文详情 | GET | `/api/v1/post/{postId}` | 含评论列表 |
| 点赞/取消点赞 | POST | `/api/v1/post/{postId}/like` | — |
| 收藏/取消收藏 | POST | `/api/v1/post/{postId}/collect` | — |
| 转发 | POST | `/api/v1/post/{postId}/share` | 记录分享渠道 |

#### 榜单模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取榜单列表 | GET | `/api/v1/ranking/list` | 支持period_type/category筛选 |
| 为作品投票 | POST | `/api/v1/ranking/vote` | 每日限10票 |
| 作品详情 | GET | `/api/v1/work/{workId}` | 含3D模型URL |
| 一键适配体模 | POST | `/api/v1/work/{workId}/fit` | 返回用户体模渲染结果 |
| 切换3D背景 | POST | `/api/v1/work/{workId}/background` | 传入背景ID |

#### 商城模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 商品列表 | GET | `/api/v1/product/list` | 支持品类/风格/价格筛选 |
| 商品详情 | GET | `/api/v1/product/{productId}` | — |
| 创建订单 | POST | `/api/v1/order/create` | 含体型数据确认 |
| 订单列表 | GET | `/api/v1/order/list` | 支持状态筛选 |
| 订单详情 | GET | `/api/v1/order/{orderId}` | 含生产进度/质检/物流 |
| 支付回调 | POST | `/api/v1/payment/callback` | 微信/支付宝回调 |
| 申请售后 | POST | `/api/v1/order/{orderId}/after-sales` | — |

#### 创作者后台

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 数据概览 | GET | `/api/v1/creator/dashboard` | 返回核心KPI |
| 作品数据明细 | GET | `/api/v1/creator/work/{workId}/stats` | 转化率/点击率/销量趋势 |
| 佣金明细 | GET | `/api/v1/creator/commission` | 可提现/待结算/历史 |
| 申请提现 | POST | `/api/v1/creator/withdraw` | — |
| 上传教程 | POST | `/api/v1/creator/tutorial/upload` | 视频+图文 |

#### 用户模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 手机验证码登录 | POST | `/api/v1/auth/login/sms` | — |
| 微信授权登录 | POST | `/api/v1/auth/login/wechat` | — |
| 更新体型数据 | PUT | `/api/v1/user/body-measurement` | 手动/AI拍照 |
| AI拍照量体 | POST | `/api/v1/user/body-measurement/ai` | 上传3张照片 |
| 更新偏好标签 | PUT | `/api/v1/user/preferences` | — |
| 我的收藏 | GET | `/api/v1/user/collections` | 支持分类 |

---

## 八、非功能需求

### 8.1 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 冷启动时间 | ≤2s | 从点击图标到首页内容可见 |
| 页面切换 | ≤300ms | 页面间跳转响应时间 |
| 信息流加载 | ≤1s | 首屏数据加载完成 |
| 3D模型加载 | ≤3s | 首次加载3D模型（含网络传输） |
| 3D交互帧率 | ≥30fps（中端）/≥60fps（旗舰） | 旋转/缩放/背景切换 |
| API响应时间 | P99≤500ms | 核心接口99分位响应时间 |
| 并发承载 | ≥10万QPS | 峰值场景（如榜单更新时） |

### 8.2 安全要求

| 安全维度 | 措施 |
|----------|------|
| 数据传输 | 全链路HTTPS + TLS 1.3 |
| 身份认证 | JWT + Refresh Token双Token机制，Token有效期2h/Refresh Token 30天 |
| 数据加密 | 体型数据AES-256加密存储；支付信息不落地存储 |
| 隐私合规 | 符合《个人信息保护法》，体型数据获取需用户明确授权；支持数据导出和账号注销 |
| 内容安全 | AI自动审核（涉政/涉黄/暴力检测）+ 人工复审；用户举报机制 |
| 防刷机制 | 投票/点赞/收藏频率限制；设备指纹 + 行为分析识别异常操作 |
| 接口安全 | 请求签名 + 时间戳防重放；敏感接口限流 |

### 8.3 可用性要求

| 指标 | 目标值 |
|------|--------|
| 系统可用性 | ≥99.95%（月度） |
| 故障恢复时间 | RTO≤30分钟 |
| 数据恢复点 | RPO≤5分钟 |
| 容灾方案 | 双机房主备切换，数据库主从复制 + 异地备份 |

### 8.4 兼容性要求

| 维度 | 要求 |
|------|------|
| iOS | iOS 15.0 及以上 |
| Android | Android 10 (API 29) 及以上 |
| HarmonyOS | HarmonyOS Next 及以上 |
| 屏幕适配 | 支持4.7英寸至7英寸屏幕；适配折叠屏展开/折叠状态 |
| 网络环境 | 支持Wi-Fi/4G/5G；弱网环境（3G/高延迟）下降级策略（图片压缩、3D降级为2D预览） |

---

## 九、版本规划

### 9.1 MVP版本（V1.0）— 预计开发周期4个月

| 模块 | 功能范围 | 优先级 |
|------|----------|--------|
| 广场 | 推文发布（图文）、信息流浏览、点赞/收藏/评论 | P0 |
| 榜单 | 周榜/月榜、基础筛选、作品详情（2D预览） | P0 |
| 商城 | 商品浏览、下单支付、订单列表、基础订单详情 | P0 |
| 用户 | 手机号登录、手动体型数据录入、基础个人信息 | P0 |
| 学习 | 课程浏览、视频播放、学习进度记录 | P1 |
| 创作者后台 | 基础数据面板（销量/转化率） | P1 |

### 9.2 V1.5版本 — MVP上线后2个月

| 模块 | 新增功能 |
|------|----------|
| 3D体验 | 3D虚拟试衣、一键适配体模、背景切换 |
| AI能力 | AI拍照量体、智能尺码推荐 |
| 社交增强 | 转发分享、关注体系、作品关联推文 |
| 榜单增强 | 投票功能、热度算法优化 |

### 9.3 V2.0版本 — V1.5上线后3个月

| 模块 | 新增功能 |
|------|----------|
| 学习中心 | 用户上传教程、设计能力认证体系 |
| 创作者生态 | 完整KPI数据面板、佣金结算、品牌孵化申请 |
| 供应链 | 全流程生产进度跟踪、质检报告在线查看 |
| 推荐系统 | 个性化推荐算法上线、趋势预测 |
| HarmonyOS | 鸿蒙原生版本发布 |

---

## 十、风险评估与应对

| 风险项 | 风险等级 | 影响 | 应对措施 |
|--------|----------|------|----------|
| 3D渲染性能不达标 | 高 | 低端设备卡顿影响体验 | 采用LOD分级策略，低端设备降级为2D预览；持续优化渲染管线 |
| AI量体精度不足 | 高 | 导致定制服装不合身，退货率上升 | 设置精度校验机制，偏差>2cm引导重拍；初期辅以人工复核 |
| 冷启动内容不足 | 中 | 广场信息流空荡，影响新用户留存 | 运营团队预置200+优质内容；邀请50+种子创作者提前入驻 |
| 柔性工厂产能瓶颈 | 中 | 订单积压导致交付延迟 | 接入3-5家备选工厂；建立产能预警机制 |
| 跨端兼容性问题 | 中 | 不同设备/系统版本出现UI异常或功能异常 | 建立自动化UI测试体系；覆盖Top 50主流机型测试 |
| 数据安全合规风险 | 高 | 体型数据泄露引发法律风险 | AES-256加密存储；定期安全审计；通过等保三级认证 |

---

## 附录

### A. 竞品参考

| 竞品 | 参考点 |
|------|--------|
| 小红书 | 广场信息流设计、推文交互、社区氛围运营 |
| 淘宝/天猫 | 商城交易流程、订单管理、物流跟踪 |
| SHEIN | 服装品类运营、用户增长策略（差异化：我们主打定制而非快时尚） |
| 得物 | 3D商品展示、社区+电商融合模式 |
| CLO 3D / Browzwear | 3D服装设计软件交互参考 |

### B. 埋点需求清单（核心事件）

| 事件名 | 触发时机 | 关键参数 |
|--------|----------|----------|
| page_view | 页面曝光 | page_name, source |
| post_click | 推文点击 | post_id, position, source |
| post_like | 推文点赞 | post_id |
| post_collect | 推文收藏 | post_id |
| post_share | 推文转发 | post_id, share_channel |
| work_view | 作品详情浏览 | work_id, source |
| work_3d_fit | 一键适配体模 | work_id, has_body_data |
| work_3d_bg_switch | 切换3D背景 | work_id, background_id |
| order_create | 创建订单 | order_id, work_id, amount |
| order_pay | 支付成功 | order_id, pay_channel, amount |
| course_start | 开始学习课程 | course_id, level |
| course_complete | 完成课程 | course_id, duration |
| dashboard_view | 查看后台数据 | user_role |

---
官方平台App的PRD已经出完了，接下来需要我帮你生成服装设计App的PRD文档吗？